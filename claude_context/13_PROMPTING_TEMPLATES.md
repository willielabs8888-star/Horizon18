# Prompting templates

When generating code:
- Provide file tree + complete files
- Provide a one-command run instruction
- Include tests and how to execute them
- Include “what changed” summary

When making decisions:
- Provide default recommendation + 2 alternatives + tradeoffs
